org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Configurações locais de desenvolvimento (NÃO adicionar segredos aqui)
# Assinatura para release deve ficar em android/key.properties (não versionado).

# Opcional: Defina seu AdMob App ID para Android via placeholder de manifesto
ADMOB_APP_ID=ca-app-pub-2940158495601484~5172283972
